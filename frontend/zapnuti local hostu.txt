cmd Temp/server_config/Apache24/bin>./httpd.exe
zapnutí db ./mysqld --no-defaults
vytváření tabulek
CREATE TABLE users
(
    user_key int NOT NULL AUTO_INCREMENT,
    email varchar(255) NOT NULL,
    password varchar(255),
    PRIMARY KEY (user_key)
);
//resetuje id - když se v prostředku vymaže tak se tímto posunou id o tu smazanou
truncate users;